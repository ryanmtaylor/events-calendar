%h3 Venue JSON Import/Export
- if flash[:errors]
  %p.errors flash[:errors]
= form_for(create_all_venues_path, :method => :post) do
  = text_area_tag(:venue_json, @venues.to_json, :rows => '10', :style => "width: 100%")
  = submit_tag("Import")